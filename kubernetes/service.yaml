apiVersion: v1
kind: Service
metadata:
  name: pawpatrol-service
spec:
  selector:
    app: pawpatrol-back
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
